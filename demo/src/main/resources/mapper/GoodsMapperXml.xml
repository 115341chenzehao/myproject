<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.czhhhh.demo.demos.springboot.mybatis.entity.GoodsXmlMapper">

    <!-- 根据商品名称模糊查询商品，并按照价格排序 -->
    <select id="searchGoodsByNameAndSortByPrice" resultType="com.czhhhh.demo.demos.springboot.mybatis.entity.Goods">
        SELECT
            id,
            name,
            price,
            description
        FROM
            goods
        WHERE
            name LIKE CONCAT('%', #{name}, '%')
        ORDER BY
            price ASC
    </select>

</mapper>